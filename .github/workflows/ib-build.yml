name: ImageBuilder Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Upload
        uses: actions/upload-artifact@v3
        with:
          name: test
          path: .